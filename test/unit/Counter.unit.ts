import { ethers } from "hardhat"
import { expect, assert } from "chai"

describe("Locker", () => {
    beforeEach(async () => {
        console.log("Executes before each test")
    })

    describe("counter functions", () => {
        it("constructor sets initial value", async () => {})
    })
})
